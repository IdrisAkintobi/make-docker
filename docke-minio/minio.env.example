# Root credentials (use strong secrets in production)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# Set MinIO server configuration
MINIO_VOLUMES="/mnt/data"

# Enable API access logging (optional)
MINIO_API_ACCESS_LOG=on
